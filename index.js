// src/index.js
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./style.css"; // only if you're using one

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
